#!/usr/bin/env bash

dotfiles=`realpath ~/.dotfiles`
deck_and_projects=`find ~/deck ~/projects/* -mindepth 1 -maxdepth 1 -type d`
all_projects=`echo "$deck_and_projects\n$dotfiles"`
session=`printf "$all_projects" | fzf`

open-session "$session"
