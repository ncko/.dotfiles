#!/usr/bin/env bash

add_cmd="things:///add"

if [[ $1 = "add" ]]; then
    full_url=$(echo "${add_cmd}?title=${2}" | sed -e s/\ /%20/g)
    open $full_url
else
    echo "Example commands:"
    echo "- things add \"get the milk\""
fi
